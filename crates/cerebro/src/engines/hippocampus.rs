/// EpisodicEngine — hippocampus.
/// Manages episodic sequences: start, step, end, recall, replay.
/// Mirrors Python engines/hippocampus.py.
pub struct EpisodicEngine;

impl EpisodicEngine {
    pub fn new() -> Self { Self }
}
