# Forward-port queue → ApexOS-RS

**Superseded 2026-08-08**: the queue now lives WITH the work, in
ApexOS-RS `BACKPORT-QUEUE.md` (their PR #337) — five entries from the
Lucida arc (W-A wave, ghost-FK repair, seed-cap spread fix, traced recall,
optional Lucida/API hardening), each anchored to its CC-RS PR.

One source of truth: port sessions edit THAT file as entries land. This
pointer is deleted when that queue empties (both repos back in lockstep).
